{# database test rendering page #}
{% block title %}Hello TestController!{% endblock %}

{% block body %}

<div class="example-wrapper">
{# conditional statement using % (remember spaces) #}
    {% if title %}
        <h1> Passed: {{ title }} </h1>
    {% else %}
        <p>No parameter passed</p>
    {% endif %}
</div>
{% endblock %}

